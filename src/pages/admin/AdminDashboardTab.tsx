// Admin Rework (stream W0-FE, Task 6) — stub so /admin resolves. Rewritten
// by stream FE-1 (Task 15): counts row, AI-today card, top consumers.
import { mutedNote } from './adminShared'

export default function AdminDashboardTab() {
  return <div style={mutedNote}>Under construction (stream FE-1).</div>
}
