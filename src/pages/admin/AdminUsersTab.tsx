// Admin Rework (stream W0-FE, Task 6) — stub so /admin/users resolves.
// Rewritten by stream FE-2 (Task 16): search/filter/sort user browser.
import { mutedNote } from './adminShared'

export default function AdminUsersTab() {
  return <div style={mutedNote}>Under construction (stream FE-2).</div>
}
