// Admin Rework (stream W0-FE, Task 6) — stub so /admin/users/:id resolves.
// Rewritten by stream FE-2 (Task 16): identity, moderation state, usage,
// suspend/ban/promote/demote actions.
import { mutedNote } from './adminShared'

export default function AdminUserDetailPage() {
  return <div style={mutedNote}>Under construction (stream FE-2).</div>
}
