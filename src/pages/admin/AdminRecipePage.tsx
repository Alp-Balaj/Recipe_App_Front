// Admin Rework (stream W0-FE, Task 6) — stub so /admin/recipes/:id resolves.
// Rewritten by stream FE-4 (Task 18): read-only admin recipe view with
// hide/restore.
import { mutedNote } from './adminShared'

export default function AdminRecipePage() {
  return <div style={mutedNote}>Under construction (stream FE-4).</div>
}
